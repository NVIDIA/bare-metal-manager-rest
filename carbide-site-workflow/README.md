# Site Workflow

Site Workflow is a Golang library that contains a set of Temporal workflows to invoke gRPC API endpoints served by Site Controller. Workflows from this library can be imported and executed by Site Agent or Cloud Workers.

## Development

To build the repo run:

    make build

To run tests:

    make test
