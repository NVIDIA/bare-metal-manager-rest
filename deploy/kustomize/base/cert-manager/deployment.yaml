# SPDX-FileCopyrightText: Copyright (c) 2021-2023 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary
#
# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

# carbide-rest-cert-manager (credsmgr) Deployment with native Go PKI
# Requires CA certificate to be provided via K8s secrets (no fallback generation)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbide-rest-cert-manager
  labels:
    app: carbide-rest-cert-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbide-rest-cert-manager
  template:
    metadata:
      labels:
        app: carbide-rest-cert-manager
    spec:
      serviceAccountName: carbide-rest-cert-manager
      terminationGracePeriodSeconds: 5
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
      containers:
        - name: cert-manager
          image: localhost:5000/carbide-rest-cert-manager:latest
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          args:
            - --use-native-pki
            - --ca-common-name=Carbide Local Dev CA
            - --ca-organization=NVIDIA
            - --ca-cert-file=/vault/secrets/vault-root-ca-certificate/certificate
            - --ca-key-file=/vault/secrets/vault-root-ca-private-key/privatekey
            - --tls-port=8000
            - --insecure-port=8001
            - --ca-base-dns=carbide.local
            - --debug
          ports:
            - containerPort: 8000
              name: https
            - containerPort: 8001
              name: http
          volumeMounts:
            - name: vault-root-ca-certificate
              mountPath: /vault/secrets/vault-root-ca-certificate
              readOnly: true
            - name: vault-root-ca-private-key
              mountPath: /vault/secrets/vault-root-ca-private-key
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - sleep 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8001
            initialDelaySeconds: 15
            periodSeconds: 15
      volumes:
        - name: vault-root-ca-certificate
          secret:
            secretName: vault-root-ca-certificate
        - name: vault-root-ca-private-key
          secret:
            secretName: vault-root-ca-private-key
