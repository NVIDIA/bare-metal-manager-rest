# SPDX-FileCopyrightText: Copyright (c) 2021-2023 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary
#
# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

# carbide-rest-site-agent (elektra) Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbide-rest-site-agent
  labels:
    app: carbide-rest-site-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbide-rest-site-agent
  template:
    metadata:
      labels:
        app: carbide-rest-site-agent
    spec:
      initContainers:
        # Create the expected directory structure for temporal certs
        - name: init-certs
          image: busybox:latest
          command:
            - /bin/sh
            - -c
            - |
              mkdir -p /etc/temporal-certs/ca /etc/temporal-certs/client
              # Copy Temporal TLS certs from the workflow-temporal-client-tls secret
              # Site-agent expects CA at /etc/temporal-certs/ca/ca.crt
              if [ -f /tmp/temporal-certs/ca.crt ]; then
                cp /tmp/temporal-certs/ca.crt /etc/temporal-certs/ca/ca.crt
                echo "Copied CA cert to /etc/temporal-certs/ca/ca.crt"
              else
                echo "ERROR: No CA cert found in /tmp/temporal-certs/"
              fi
              if [ -f /tmp/temporal-certs/tls.crt ]; then
                cp /tmp/temporal-certs/tls.crt /etc/temporal-certs/client/tls.crt
                cp /tmp/temporal-certs/tls.key /etc/temporal-certs/client/tls.key
                echo "Copied client certs from Temporal TLS secret"
              else
                echo "ERROR: No client cert found in /tmp/temporal-certs/"
              fi
              ls -la /etc/temporal-certs/ca/ /etc/temporal-certs/client/
          volumeMounts:
            - name: temporal-certs
              mountPath: /etc/temporal-certs
            - name: temporal-tls-source
              mountPath: /tmp/temporal-certs
              readOnly: true
      containers:
        - name: site-agent
          image: localhost:5000/carbide-rest-site-agent:latest
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 2112
              name: metrics
          envFrom:
            - configMapRef:
                name: carbide-rest-site-agent-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: carbide-secrets
                  key: db-password
          volumeMounts:
            - name: site-registration
              mountPath: /etc/sitereg
              readOnly: true
            - name: carbide-certs
              mountPath: /etc/carbide
              readOnly: true
            - name: temporal-certs
              mountPath: /etc/temporal-certs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: site-registration
          secret:
            secretName: site-registration
        - name: carbide-certs
          secret:
            secretName: carbide-tls-certs
            optional: true
        - name: temporal-certs
          emptyDir: {}
        - name: temporal-tls-source
          secret:
            secretName: workflow-temporal-client-tls