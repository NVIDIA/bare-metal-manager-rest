# Carbide REST API Schema

This repo contains OpenAPI schema for Carbide REST API endpoints.

To view a rendered/browsable version of the schema, make sure you have docker and run the following:

    ./serve.sh
